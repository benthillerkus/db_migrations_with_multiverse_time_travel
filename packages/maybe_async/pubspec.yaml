name: maybe_async
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

resolution: workspace

# Add regular dependencies here.
dependencies:
  maybe_async_annotations: ^1.0.0
  source_gen: 2.0.0
  build_config: ^1.1.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
